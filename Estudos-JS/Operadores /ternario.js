// Variavel idade recebe o valor de 20
let idade = 20;

// A variavel recebe o valor "true" ou "falso" dependendo da idade,
// Se for maior ou igual a 8, sendo "true", exibe a primeira mensagem, caso contrario
// Sendo "false", exibe a segunda mensagem
let verificarIdade = idade >= 18 ? "Você é maior de idade" : "Você é menor de idade";

// Imprime o que foi armazenado na variavel
console.log(verificarIdade);
