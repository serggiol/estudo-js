let x = 5; // Declaração de uma variável "x" com o valor 5
let y = 10; // Declaração de uma variável "y" com o valor 10
let z = "5"; // Declaração de uma variável "z" com o valor "5" como string

console.log(x == y); // O operador "==" compara se os valores são iguais. Nesse caso, retorna "false" porque "x" é diferente de "y".
console.log(x != y); // O operador "!=" compara se os valores são diferentes. Nesse caso, retorna "true" porque "x" é diferente de "y".
console.log(x < y); // O operador "<" compara se o valor da esquerda é menor que o valor da direita. Nesse caso, retorna "true" porque "x" é menor que "y".
console.log(x > y); // O operador ">" compara se o valor da esquerda é maior que o valor da direita. Nesse caso, retorna "false" porque "x" é menor que "y".
console.log(x <= 5); // O operador "<=" compara se o valor da esquerda é menor ou igual ao valor da direita. Nesse caso, retorna "true" porque "x" é igual a 5.
console.log(y >= 10); // O operador ">=" compara se o valor da esquerda é maior ou igual ao valor da direita. Nesse caso, retorna "true" porque "y" é igual a 10.
console.log(x === z); // O operador "===" compara se os valores são iguais e do mesmo tipo. Nesse caso, retorna "false" porque "x" é um número e "z" é uma string.
console.log(x !== z); // O operador "!==" compara se os valores são diferentes ou de tipos diferentes. Nesse caso, retorna "true" porque "x" é um número e "z" é uma string.
